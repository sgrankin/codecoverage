fund=false
audit=false
# Only resolve package versions at least this many days old (npm 11.10+;
# older npm warns and ignores). Cheap supply-chain defense: compromised
# releases are typically caught and unpublished within hours or days.
# Dependabot has its own equivalent cooldown in .github/dependabot.yml.
min-release-age=7
